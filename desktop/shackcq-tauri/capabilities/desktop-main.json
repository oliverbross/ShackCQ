{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-main",
  "description": "Minimal permissions for the bundled ShackCQ operating window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
