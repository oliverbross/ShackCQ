# Home provider ownership and contracts

Reviewed 2026-08-20 against Android production code through Task 2B2B. `filesDir` means app-private storage. Provider state must say `LIVE`, `CACHED`, `DEGRADED`, `STALE`, or `UNAVAILABLE` (or the equivalent typed state); a successful parse is required before last-good data is replaced.

Task 2B2 adds no unbounded public-provider poller:

- RBN is typed `RBN` evidence from the existing operator-configured retail DX-cluster socket. ShackCQ does not connect to the official raw RBN Telnet nodes.
- personal WSPR delegates to the same `PskReporterRepository` used by PSK Reporter, adding only `mode=WSPR`; sender/receiver semantics, `nolocator=1`, caching, cooldown and backoff remain shared;
- regional WSPR.live is `UNAVAILABLE_POLICY`, default disabled, and has no production HTTP path;
- IBP is a versioned/hash-recorded local NCDXF/IARU schedule manifest, with no network refresh and no claim that a scheduled beacon was heard;
- Band Health is a local deterministic reducer over bounded live evidence; a compact indexed `qso_projection` aggregate supplies separate station/band/mode/comparable-UTC-window historical context. It is not a propagation forecast.
- Empirical outlook is a local deterministic reducer owned by `NeuralOutlookController`. It receives immutable snapshots from these existing owners, never fetches a provider, stores only compact aggregates in `neural-dx.sqlite`, and shares one snapshot across Neural DX, Home/map and Log Intelligence.

Task 2B2 scale contracts are RBN buffer 1,000/raw and 120/presentation-map, personal WSPR 100/map, PSK provider response 500/direction, and IBP 18 points plus five current paths. Repeated Band Health contributors are capped at three observations per source/call/receiver key.

| Home input | Data source and native owner | Request / response contract | TTL; manual limit; maximum response | Cache and last-good behaviour | Attribution / openhamclock.com / shared ownership |
|---|---|---|---|---|---|
| Station, radio and TX safety | Local settings, selected Wavelog station and live CAT; `AppController`, `WavelogController`, radio controllers | No public HTTP. Typed local/CAT state only. | Event-driven; operator actions remain bounded. | App-private settings and existing controller stores; no fabricated fallback. | ShackCQ; no OHC dependency; shared with Radio and Logbook. |
| Home and related native basemaps | OpenFreeMap network styles for DARK and LIGHT plus OpenFreeMap Liberty for Portable/POTA/Progress; `HamClockHomeMap` and native map screens | DARK and LIGHT load attributed HTTPS OpenFreeMap styles; no key/demo credential exists. | Viewport-driven MapLibre requests only; no Home retry/prefetch loop. Home low-data constructs no MapLibre view. Provider/style failure shows Map Data over the local background. | MapLibre owns normal HTTP cache; ShackCQ does not create an offline tile pack or override provider cache headers. MapLibre attribution/logo remain enabled. SATELLITE/TERRAIN remain unavailable. |
| Home overlay snapshots | Typed native controllers; `HamClockHomeMap` + `HamClockRegistries` | Stable per-layer GeoJSON source/layer IDs updated from bounded immutable snapshots. Every registry layer has sanitized current/degraded/empty/unavailable truth. | Caps are declared per layer; camera writes debounce 600 ms; one-minute celestial/map snapshot cadence. | Existing controller last-good state; no second provider or unbounded marker cache. Unchanged GeoJSON fingerprints are not rewritten. | Source label and unavailable reason come from the layer registry; no OHC data proxy. |
| DX cluster | User-configured DX cluster; `FeatureController` | Existing cluster session and normalized `AndroidDXSpot` rows with typed `DISABLED/CONNECTING/CONNECTED/RETRY_WAIT/ERROR` transport truth. | Active Home window/cadence/cap/filter settings; no blind retry. | Controller retains its latest spot and sanitized transport error independently from solar state. | Cluster operator/provider; no OHC dependency; DX workspace authoritative. |
| DX News | DX-World RSS plus shared NG3K ADXO; `DxNewsRepository` | HTTPS-only, maximum three redirects, RSS/XML type validation, 1,000,000-byte cap; normalized typed news merged with the existing `DxpeditionScheduleProvider` feed. DXNews.com is `UNAVAILABLE` because no stable direct structured contract was verified. | 30 min TTL; 10 min manual limit; merged output cap 40; conditional requests and shared in-flight keys. | `filesDir/hamclock-public/dxnews-dxworld.{json,meta}`; malformed/empty refresh cannot replace last good. URL, source/title and callsign/title similarity dedup retain distinct same-callsign stories. | DX-World and NG3K; no OHC dependency; one repository shared by Home and DX, with no second NG3K request. |
| Local weather | Open-Meteo; `NeuralDxController.refreshWeather` | HTTPS GET forecast JSON for station coordinates; required current-weather fields are parsed. | 30 min; Home SYNC is disabled while refresh is active; bounded by `readUrl`. | `filesDir/neural-dx-cache/weather.json`; invalid refresh retains last good and reports error. | Open-Meteo; no OHC dependency; shared with DX intelligence. |
| PSK Reporter / signal | PSK Reporter direct query; `PskReporterRepository` via `NeuralDxController` | HTTPS GET JSONP with exact callback validation; `senderCallsign` for Being Heard and `receiverCallsign` for Hearing; sender/receiver identity, time, frequency, mode and SNR are validated. | 5 min TTL/manual limit; 2,000,000 bytes; provider request limit 500 per direction; active cadence cannot be below 5 min. Retry-After raises the backoff. | Direction/callsign/window-keyed files under `filesDir/hamclock-public`; last good retained; generation checks discard late disabled/reconfigured results. | PSK Reporter; no OHC dependency; one repository shared with Home/DX. Mutual means the same remote callsign and band inside the selected window. |
| Personal WSPR evidence | PSK Reporter through the shared `PskReporterRepository`, `mode=WSPR`; `NeuralDxController.refreshWspr` | Same validated sender/receiver JSONP contract as PSK. Regional WSPR.live is `UNAVAILABLE_POLICY` and has no request path. | 5 min provider cadence; local direction/band/SNR/cap/geometry changes reproject without a request. | Shared PSK last-good directional caches. | PSK Reporter; no OHC dependency; shared with Home/DX. |
| DXpeditions | NG3K ADXO; `DxpeditionScheduleProvider` | HTTPS GET HTML/text, parsed to bounded callsign/date/band/mode rows. | 30 min; no separate Home manual control; 2,000,000 bytes. Concurrent calls share key `dxpedition-schedule`. | `filesDir/hamclock-public/dxpeditions-ng3k.{json,meta}`; parse must yield rows; failure is `STALE` or `UNAVAILABLE`. | NG3K ADXO; no OHC dependency; shared with Operations. |
| Contest calendar | WA7BNM Contest Calendar; `ContestCalendarProvider` | HTTPS GET RSS/XML, parsed to active/upcoming contest rows. | 30 min; no separate Home manual control; 1,500,000 bytes. Concurrent calls share key `contest-calendar`. | `filesDir/hamclock-public/contests-wa7bnm.{json,meta}`; invalid downloads never replace last good. | WA7BNM; no OHC dependency; shared with Operations. |
| Solar indices | NOAA SWPC; `FeatureController` | HTTPS GET NOAA summary/K-index products plus `json/solar-cycle/observed-solar-cycle-indices.json`; SSN selects the greatest plausible non-future `YearMonth` and bounded `observed_swpc_ssn`, independent of array order. | Controller-owned hourly freshness gate; Home SYNC requests one refresh. Responses use the existing bounded reader. | SFI/A/Kp publish before the optional SSN stage. SSN failure retains the last valid value/month and exposes only a sanitized SSN error; it cannot mark fresh core indices stale. | NOAA SWPC; no OHC dependency; shared with DX/Progress. |
| GOES X-ray and celestial | NOAA SWPC GOES plus local sun/Moon phase calculations; `SolarCelestialProvider` | HTTPS GET JSON array; primary long-channel points required. Sun position, daylight and Moon phase are local calculations. No sublunar map geometry is claimed. | 5 min; no separate manual control; 5,000,000 bytes. Key includes rounded station coordinates. | `filesDir/hamclock-public/goes-xray.{json,meta}`; X-ray failure yields `STALE`/`UNAVAILABLE`, while local celestial calculations remain labelled available. | NOAA SWPC, NASA SDO metadata; no OHC dependency; shared with Home only. |
| Propagation path | Public OpenHamClock propagation endpoint; `HamClockPropagationRepository` | HTTPS GET query (`deLat`, `deLon`, `dxLat`, `dxLon`, `mode`, `power`, `antenna`). JSON must contain 1–64 `currentBands`; every row requires bounded `band`, finite `freq`, reliability 0–100, bounded `snr` and non-empty `status`. | 10 min; path changes trigger refresh; 1,000,000 bytes. Same normalized path key shares one active result. | `filesDir/hamclock-propagation.json`; only validated payloads replace cache. Fresh cache is `CACHED`; failed refresh with prior data is `STALE`; otherwise `UNAVAILABLE`. | OpenHamClock contributors; **depends on openhamclock.com**; shared with Home/DX path display. |
| POTA | POTA public spot/park feeds; `PotaRepository` | HTTPS GET JSON spots and CSV park catalogue, normalized to `PortableSpot`/park rows. | Spot cadence owned by repository; Home invokes existing refresh lifecycle only. | App-private POTA caches; last-good provider status is visible. | POTA; no OHC dependency; Portable workspace authoritative. |
| WWFF / SOTA / WWBOTA | WWFF public spot/agenda feeds and permitted SOTA catalogue; `PortableRepository` | HTTPS JSON/CSV contracts defined in `PortableModels.kt`; deprecated/private live SOTA access is not used. | Repository-owned cadence; one Home portable refresh; bounded readers. | App-private portable caches with provider status and age. | Respective programmes; no OHC dependency; Portable workspace authoritative. WWBOTA Home preference is planned, not active. |
| Satellites | CelesTrak, SatNOGS and local pinned SGP4; `SatelliteOperationsController`, `SatelliteProviderRepository` | HTTPS CSV/JSON TLE/transmitter metadata; typed validation before orbital calculation. Home positions reuse only validated cached elements and pinned `NativeSatellite`; no Home orbital download exists. | Provider/controller cadence; operator force refresh is controller-gated. While Home is visible and foreground, a single non-overlapping local job recalculates at 45-second cadence, favourites plus selected with an eight-row fallback, capped at 40. | App-private element/metadata caches with explicit state, position generation age and stale-element truth. Home station/Wavelog grid is distinct from the persisted Operations observer. | CelesTrak/SatNOGS; no OHC dependency; Satellite/Operations workspaces authoritative. The legacy Neural DX satellite path remains only for its dedicated full-DX refresh scope. |
| Logged QSOs | `QsoDatabase` and optional Wavelog synchronization | Indexed compact `qso_projection` query selecting only ID/call/frequency/band/mode/country/grid/time; no canonical JSON decode or public request from Home. | Change-token driven; maximum 120 recent rows for the map. | Local database remains authoritative; no network fallback invented. | ShackCQ/Wavelog; no OHC dependency; shared with Logbook/Progress. |
| Lightning and other DX context | Public sources owned by `NeuralDxController` | Bounded HTTPS responses parsed before state update. | Controller lifecycle; Home has one disabled-while-active SYNC action. | `filesDir/neural-dx-cache`; last-good truth retained. | Source-specific attribution in UI/code; no OHC dependency; shared with DX. |

## Coalescing contract

`HamClockInFlightCoalescer` is shared by the native Home public-provider group. One active request/result exists per exact key, distinct keys may execute independently, an interrupted observer does not cancel the shared future, exceptional completion removes the key, and completed results are not retained. Provider TTL and last-good disk caches remain authoritative.

## Manual refresh boundary

Home exposes bounded operator refresh actions for the existing controller lifecycle plus DX News and PSK. News enforces ten minutes and PSK five minutes; both return cached/last-good truth inside the limit. Contest, DXpedition and celestial providers refresh automatically. No provider request can tune, transmit, key PTT, update runtime source code, or mutate an upstream pin.

Task 2B2A completes the shared future before exact `(key, future)` removal. RBN stays on the configured retail cluster, retains line and receipt time, batches publication and is not also ingested as a generic cluster spot. Personal WSPR shares PSK Reporter; local filters and station geometry reproject without requests. Regional WSPR.live remains `UNAVAILABLE_POLICY`. IBP scheduled reference and matching observed cluster/RBN evidence are separate. OpenFreeMap public MapLibre styles provide Home geography with the existing Map Data fallback on provider failure.

Task 2B2B adds a two-second non-overlapping RBN maintenance publication so quiet feeds expire without unrelated cluster traffic. Typed RBN freshness uses only the latest RBN epoch and reports endpoint, bounded raw/filtered counts and sanitized errors. The skimmer is the selectable point; geometry prefers stream, current-station and cached callbook grids before approximate CTY fallback. PSK and personal WSPR retain reports during zero-request station-grid reprojection and use one combined direction-state reducer. DX News always refreshes the shared TTL/coalesced NG3K authority on direct DX launch. Home, DX and Log Intelligence consume the same Band Health snapshot and compact projection history.

## Finish-line providers

| Outcome | Exact contract | Bounds, cadence and cache | Truth and attribution |
|---|---|---|---|
| Solar wind | `services.swpc.noaa.gov/products/summary/solar-wind-speed.json` | 1.5 MB; Home foreground refresh no faster than 15 minutes; combined atomic last-good cache | NOAA SWPC; speed remains missing when absent |
| IMF Bz | `services.swpc.noaa.gov/products/summary/solar-wind-mag-field.json` | 1.5 MB; same shared 15-minute refresh/cache | NOAA SWPC; no derived substitute |
| Proton/radiation state | `services.swpc.noaa.gov/json/goes/primary/integral-protons-3-day.json` | 3 MB; at most 20,000 rows; exact >=10 MeV channel | NOAA SWPC GOES; S-scale is derived only from the measured proton flux thresholds |
| Active alerts | `services.swpc.noaa.gov/products/alerts.json` | 1.5 MB; at most 2,000 input rows and eight bounded first-line messages | NOAA SWPC |
| Aurora | `services.swpc.noaa.gov/json/ovation_aurora_latest.json` | 5 MB; validated timestamp/coordinates; at most 720 parsed cells and 180 rendered fills; 30-minute cache cadence | NOAA SWPC OVATION Aurora Forecast; no fabricated oval |
| Solar imagery | NASA SDO latest AIA 171/193/304 and HMI continuum/magnetogram JPEGs | 4 MB, 8,192-pixel input guard, downsampled to at most 2,048 pixels, one last-good image per channel, 15-minute manual cooldown; disabled in low-data mode | NASA SDO/AIA and SDO/HMI |
| Contest QSOs | Local `qso_projection` using `qso_projection_contest_idx` and the selected contest time window | 200 projected rows; exact station/contest/band/mode/confirmation predicates; no full-log load | ShackCQ local/Wavelog revision truth |
| Satellite tracks/footprints | Existing `SatelliteOperationsController` and pinned `NativeSatellite` SGP4 | Same 45-second foreground job; at most four tracks and four 49-point footprints; no second provider/controller | CelesTrak/SatNOGS element age plus local SGP4 generation age |

The official ITU-R-HF repository is not a runtime provider. No source or data is downloaded because redistribution permission is unresolved; downloaded executable code is prohibited.
