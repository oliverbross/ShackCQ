# Tablet Acceptance Sweep 1

## Frozen execution boundary

- Frozen `main`: `fb04d52df0c9ccc305125449bb188ef8e3f0185e`
- Feature branch: `fix/tablet-acceptance-sweep-1`
- Worktree: `/Users/oliver/Documents/Projects/ShackCQ/shackcq-tablet-acceptance-sweep-1`
- Baseline QSO database: schema 16, projection contract 5
- Main is not modified or merged by this programme.
- Physical, authenticated-service, audio and RF evidence remain separate from source/build evidence.

## Acceptance issue matrix

Status legend: `FIXED` means the named source correction is implemented. Local, hosted, package and physical evidence remain distinct and are recorded below; a fixed row alone is not a physical-device claim.

| ID | Screen | Observed problem | Production owner/file | Verified root cause | Planned correction | Validation evidence | Final status |
|---|---|---|---|---|---|---|---|
| HOME-01 | Home | Operations consumes a duplicate dashboard row | `HamClockHomeScreen.kt` | Wide and compact branches directly compose `OperationsHomeSummary`; navigation/deep links already exist separately | Remove both compositions and the summary UI while retaining Operations controller/deep links | Home migration/UI tests; tablet screenshot | FIXED — source implementation complete; validation tracked below |
| HOME-02 | Home | Groups.io consumes a duplicate dashboard row | `HamClockHomeScreen.kt` | Wide and compact branches directly compose `GroupsIoHomeCard` whenever enabled; Groups.io has an independent destination | Remove both compositions/card without changing controller or destination | Home composition test; Groups.io navigation test | FIXED — source implementation complete; validation tracked below |
| HOME-03 | Home/About | Product/version/build metadata clutters Home | `HamClockHomeScreen.kt`, `MainActivity.kt`, `SystemHealthCentre.kt` | `HamClockHeader` directly renders a `BuildConfig.VERSION_NAME`/OpenHamClock provenance subtitle; About has no equivalent complete copyable build block | Keep concise station identity; move accurate version/code/SHA/channel/schema/provider provenance to About | Home/About unit and UI assertions | FIXED — source implementation complete; validation tracked below |
| HOME-04 | Home | Shack entry is an ordinary `SHACK` button | `HamClockHomeScreen.kt`, `finishline/HamClockShackDisplay.kt` | `ShackButton` is a minimum-size text button with no profile/theme/status context | Add a themed tablet entry card with icon, ENTER semantics, content description and compact fallback; reuse existing display state | Light/dark/compact Compose tests; physical screenshot | FIXED — source implementation complete; validation tracked below |
| SET-01 | Settings | Large `CONTEST · N1MM · DX CHASER` summary card dominates top-level Settings | `KeyerContestDxChaserUi.kt`, `MainActivity.kt` | Settings directly composes `IntegratedOperationsSettings`; individual feature owners already exist | Remove summary card and expose deep-linkable natural subsections | Settings navigation tests | FIXED — source implementation complete; validation tracked below |
| SET-02 | Navigation/Settings | EQ visibility is not AUTO/SHOW/HIDE | `MainActivity.kt`, `ConfigurationRecovery.kt`, existing radio-family authority | Navigation only special-cases Flex; there is no persisted visibility policy or unsupported SHOW state | Persist AUTO/SHOW/HIDE, derive AUTO from selected Elecraft family, redirect safely, block EQ CAT on unsupported SHOW | Policy/config recovery tests | FIXED — source implementation complete; validation tracked below |
| SET-03 | Navigation/Settings | Contest destination cannot be disabled safely | `MainActivity.kt`, `ContestRuntime`, `ConfigurationRecovery.kt` | Contest is always included in destination navigation and lacks a persisted visibility toggle | Add default-enabled setting; on disable stop/pause runtime, clear arms, retain database/config and redirect | Visibility/runtime/config tests | FIXED — source implementation complete; validation tracked below |
| CLR-01 | Radio/DX/Band Maps/Neural/Home | CS/DS meanings use screen-local colours | `MainActivity.kt`, `HamClockHomeScreen.kt`, `BandMapScreen.kt`, `NeuralDxScreen.kt` | Renderers use local `Hc*`/screen constants rather than one preference-backed classification adapter | Add a shared presentation palette/token adapter and retain text/shape status | Golden mapping and consumer tests | FIXED — source implementation complete; validation tracked below |
| CON-01 | Contest | Top actions duplicate destination navigation | `KeyerContestDxChaserUi.kt` | Integrated header renders Radio/Logbook/Log Intelligence/Settings actions above the Contest workspace | Replace with compact operational state and safe Stop only | Contest UI test | FIXED — source implementation complete; validation tracked below |
| CON-02 | Contest | Setup/Logging/Review/Network tabs are unreadable in dark theme | `contest/ContestWorkspace.kt` | Minimal tab row does not define selected/unselected/disabled theme colours or robust semantics | Use Material colour scheme, indicator and accessibility labels | Light/dark Compose tests | FIXED — source implementation complete; validation tracked below |
| CON-03 | Contest Setup | Setup is explanatory scaffold, not a session editor | `ContestSetupScreen.kt`, existing `ContestDefinition`/`ContestSessionStore`/`ContestSessionController` | Screen is 25 lines and exposes only basic start/stop callbacks despite existing 13-definition/rule authorities | Add definition-driven draft editor, validation, lifecycle controls and calendar prefill using existing stores | Definition/validation/session tests | FIXED — source implementation complete; validation tracked below |
| CON-04 | Contest Logging | Logging is a callsign/exchange scaffold | `ContestLoggingScreen.kt`, `ContestRuntime`, `QsoMutationCoordinator` adapter | Screen is 26 lines and does not render the existing dynamic exchange/evaluation/context state | Add configurable tablet cockpit and rule-driven QSO entry; preserve canonical mutation/serial authority | Dynamic field, canonical save and layout tests | FIXED — source implementation complete; validation tracked below |
| CON-05 | Contest Logging | Multipliers/score/rates are not operator-usable panels | `ContestLoggingScreen.kt`, score/multiplier/rate engines | Existing engine snapshots are reduced to a small summary instead of dedicated bounded panels | Surface engine-owned score, multiplier, rate, recent-QSO and review counts | Engine-to-UI snapshot tests | FIXED — source implementation complete; validation tracked below |
| CON-06 | Contest Review | Review is explanatory scaffold | `ContestReviewScreen.kt`, `ContestCanonicalQsoReader.kt`, export/mutation owners | Screen is 19 lines with no paging, filters, canonical edit/delete or export validation presentation | Add bounded review workspace and canonical actions/exports | Paging/filter/export/mutation tests | FIXED — source implementation complete; validation tracked below |
| CON-07 | Contest Network | N1MM screen is explanatory scaffold | `ContestNetworkScreen.kt`, existing N1MM runtime/store | Screen is 25 lines and omits peer/trust/counters/review/arm state already owned by runtime | Add safe status/control workspace; keep default off/unarmed and blocked command policy | N1MM safety/UI tests | FIXED — source implementation complete; validation tracked below |
| CON-08 | Band Maps/Contest | Contest S&P preset appears outside active Contest context | `BandMapStateStore.kt`, `BandMapScreen.kt`, `ContestRuntime` snapshot | Built-in preset is ordinary persisted UI state with no RUNNING/S&P availability gate | Gate selection/display and restore previous non-Contest preset on pause/end | Preset lifecycle tests | FIXED — source implementation complete; validation tracked below |
| BMAP-01 | Band Maps | Visible band strip includes out-of-scope bands | `BandMapDomain.kt`, `BandMapStateStore.kt`, `BandMapScreen.kt` | UI iterates canonical `BandMapBand.entries`; persisted selections are not filtered to 160m–23cm | Introduce reviewed visible-band list and migrate selections/order safely | Codec/selector tests | FIXED — source implementation complete; validation tracked below |
| BMAP-02 | Band Maps | Frequency labels are incomplete across four layouts | `BandMapLayoutEngine`, `BandMapScreen.kt` | Tick generation exists but current lane/card renderers do not consistently draw labelled major/minor axes | Add adaptive labelled axes to vertical, horizontal, grid and single layouts | Four-layout rendering tests | FIXED — source implementation complete; validation tracked below |
| BMAP-03 | Band Maps | No typed local operating-segment display plan | `BandMapDomain.kt`, `BandMapStateStore.kt` | Existing segment profile represents coarse whole/CW/digital/phone bounds, not reviewed IARU guidance, legend, version or custom region plan | Add typed Region 1/2/3 guidance table, custom segment and provenance; label non-regulatory truth | Domain/codec tests | FIXED — source implementation complete; validation tracked below |
| BMAP-04 | Band Maps | Frequency maps do not truly zoom or pan | `BandMapScreen.kt`, `BandMapStateStore.kt` | Renderers always use a static display segment; no persisted viewport or gesture/pointer/keyboard controls exist | Add bounded viewport model, gestures/buttons/reset and per-band/layout persistence | Clamp/persistence/interaction tests | FIXED — source implementation complete; validation tracked below |
| BMAP-05 | Band Maps | Callsign labels overlap at density | `BandMapLayoutEngine` | Placement uses one-dimensional fixed minimum spacing and has no measured width, lane allocation or stack result | Add deterministic bounded lane/stack collision engine with priority ordering | 20k-observation and non-overlap tests | FIXED — source implementation complete; validation tracked below |
| BMAP-06 | Band Maps Grid | Grid cards lack useful miniature map information | `BandMapScreen.kt` | Grid delegates to the same compact lane renderer without miniature labelled axis/segments/counts/selected-frequency handoff | Add adaptive grid card renderer and selected-frequency expansion | Grid UI/state test | FIXED — source implementation complete; validation tracked below |
| BMAP-07 | Band Maps Single | Expanded view is an undifferentiated lane | `BandMapScreen.kt` | `SingleExpanded` delegates to `HorizontalLane` and does not own expanded markers/details/viewport | Add full axis, segments, RX/TX/split/passband markers, stacks and detail | Single layout UI/state test | FIXED — source implementation complete; validation tracked below |
| BMAP-08 | Band Maps Multi | Multi layouts lose per-band viewport/context clarity | `BandMapScreen.kt`, `BandMapStateStore.kt` | Current rows/columns render whole segments with shared fixed label behavior and no per-band viewport | Add per-band ranges, current-band emphasis, expansion and collision rendering | Multi-layout state tests | FIXED — source implementation complete; validation tracked below |
| LOG-01 | Log Intelligence | Switching Wavelog OM0RX to VK8RX leaves OM0RX statistics | `ProgressController.kt`, `ProgressModels.kt`, `LogIntelligenceRepository.kt`, `MainActivity.kt` | SQL can filter profile/callsign, but controller input has no log mode/binding/remote-station identity and defaults from local station context; generations can therefore reuse the wrong scope | Add one immutable log-scope key from actual local/Wavelog authorities; cancel/reject stale generations and carry scope to drill-through | Two-station Local/Wavelog fixture tests | FIXED — source implementation complete; validation tracked below |
| LOG-02 | Log Intelligence | `LAST YEAR` is absent | `ProgressModels.kt`, `LogIntelligenceRepository.kt`, `ProgressScreen.kt` | Period enum/SQL boundaries do not expose the previous completed UTC calendar year | Add exact UTC previous-year range and visible dates | Boundary/drill-through tests | FIXED — source implementation complete; validation tracked below |
| LOG-03 | Log Intelligence | KPI cards waste tablet space | `ProgressScreen.kt` | KPI strip uses broad fixed card allocation rather than adaptive content-sized grid | Add adaptive compact metric grid with phone fallback | Lenovo/large-font Compose test | FIXED — source implementation complete; validation tracked below |
| LOG-04 | Log Intelligence | Charts use a mostly single blue treatment | `ProgressScreen.kt` | Chart renderers share hard-coded primary colours without stable semantic series tokens | Add stable theme-aware accessible chart palette and selection/detail semantics | Palette/UI tests | FIXED — source implementation complete; validation tracked below |
| LOG-05 | Log Intelligence | UTC day/hour heatmap lacks complete intensity/legend semantics | `ProgressScreen.kt`, `LogIntelligenceRepository.kt` | Counts exist, but current presentation is not the required stable labelled zero-to-max legend/accessibility contract | Complete sequential scale, zero state, legend, labels and spoken counts | Monotonic intensity/UI test | FIXED — source implementation complete; validation tracked below |
| LOG-06 | Contact Map | Valid worked grids can produce `No valid contact grids` | `LogIntelligenceRepository.kt`, `ProgressModels.kt`, QSO projection/import mapping | Aggregates count `grid_norm`, but contact-point projection/normalization is not coupled to the scoped SQL snapshot or bounded repair of missing projection grids | Add 4/6/8 grid normalization, scoped exact points, invalid diagnostics and bounded projection repair path | Locator/scope fixture tests | FIXED — source implementation complete; validation tracked below |
| UI-01 | Presets | `RADIO OFFLINE` touches `ADD PRESET` | `MainActivity.kt` `Header`/`PresetsScreen` | Generic header status and fixed action row compete for width without flow/stack breakpoint | Add responsive header/action layout with safe spacing | Tablet/large-font UI test | FIXED — source implementation complete; validation tracked below |
| UI-02 | DX | Feed panel too narrow; UTC/band/mode/distance truncate | `MainActivity.kt` `DXScreen`/DX feed row | Wide layout uses existing fixed weights and row fields compete without essential-column minimums | Increase feed weight ~20%, reserve essential metadata and ellipsize comment last | Wide/phone layout tests | FIXED — source implementation complete; validation tracked below |
| PORT-01 | Portable On Air/Map | Callsigns are not coordinate-anchored labels | `PortableChaseScreen.kt` MapLibre integration | Map renders point annotations/selection without a reviewed GeoJSON SymbolLayer label/declutter contract | Add one coordinate-tied source/layer with zoom threshold, selected override and bounded clustering | Camera-change/map contract test | FIXED — source implementation complete; validation tracked below |
| PORT-02 | Portable Places | POTA/SOTA place cards remain one column on tablet | `PortableChaseScreen.kt` | Places content uses a single linear list regardless of available width | Add adaptive two-column grid and one-column phone fallback | Responsive Compose test | FIXED — source implementation complete; validation tracked below |
| PORT-03 | Portable browser | POTA/SOTA pages fail because JavaScript is globally disabled | `InAppBrowser.kt`, portable link owners | WebView applies one global hardened policy with JS/DOM disabled and has no typed host policy | Add explicit reviewed HTTPS host policy with minimum features; prevent redirect privilege inheritance | Browser policy/redirect tests | FIXED — source implementation complete; validation tracked below |
| OPS-01 | Activation Planner | Nearby result coverage reflects incomplete subsets | `OperationsScreen.kt`, `PortableRepository.kt` | Planner consumes current POTA/SOTA query results and live WWFF spots instead of three catalog authorities | Separate catalog snapshots from live spots and expose provider truth | Cross-border catalog fixture test | FIXED — source implementation complete; validation tracked below |
| OPS-02 | Activation Planner | Provider/cache provenance is not complete | `PortableRepository.kt`, Operations UI | Current sources expose partial counts/status but not a uniform last-good/version/licence/invalid-row contract | Add structured bounded provider metadata and last-good truth; show unavailable precisely | Provider parsing/cache tests | FIXED — source implementation complete; validation tracked below |
| OPS-03 | Activation Planner | Radius/filtering cannot prove complete cross-border results | `OperationsScreen.kt` | Filtering is performed over pre-filtered controller results and truncates to 20, so radius inclusivity/coverage cannot be distinguished from source gaps | Filter canonical catalog snapshots off-main with inclusive great-circle distance, dedupe and bounded paging | 250 km cross-border fixture test | FIXED — source implementation complete; validation tracked below |
| OPS-04 | Activation Planner | WWFF can be an empty tab without provider truth | `OperationsScreen.kt`, `PortableRepository.kt` | WWFF planner rows are derived from live agenda spots rather than a first-class catalog/status | Add WWFF catalog authority/status/counts or explicit unavailable state shared with Portable | WWFF availability tests | FIXED — source implementation complete; validation tracked below |
| GRP-01 | Groups.io | Message/topic/search/offline rows omit actual dates and times | `groupsio/GroupsIoFeature.kt`, `GroupsIoPhase2Ui.kt` | Models/database retain server epochs, but row renderers do not consistently format them and have no `TIME UNKNOWN` contract | Add compact local date/time plus detail UTC/relative secondary text using stored server epoch only | Online/offline timestamp tests | FIXED — source implementation complete; validation tracked below |
| PERF-01 | Cross-cutting | Risk of full-log/main-thread/duplicate-controller regressions | Existing repositories/controllers | Several corrections cross scopes, but canonical projection/paging/controller owners already exist and must remain single | Keep `qso_projection`, bounded queries, cancellation and existing controller ownership; add repository scans/performance fixtures | Scans, scale tests, controller inventory | FIXED — source implementation complete; validation tracked below |
## Initial evidence

- `origin/main`, local `main`, and the feature worktree started at the frozen SHA.
- All 22 pre-existing worktrees were clean before this worktree was added.
- Existing debug APK: 117,925,723 bytes; no existing debug AAB was present.
- Upstream watchers: Wavelog `NO CHANGE`; MSHV `NO_REVIEW`; OpenHamClock stable/release/licence unchanged with preview review required; Nexus review required for an upstream package/code delta. No upstream source was absorbed.
- Expected Lenovo TB373FU was visible over authorised wireless ADB; no install or state-affecting device action has occurred.

## Operator-authorized SOTA cluster extension

- Portable SOTA uses the receive-only GM4LLD SOTA Cluster at `cluster.sota.org.uk:7300`.
- Login is the username currently configured in DX-cluster settings; it is not hard-coded or reproduced in evidence.
- The connection exists only while Portable is foregrounded, never posts a spot, uses bounded reconnect/backoff, deduplicates to at most 250 rows and expires each row after one hour.
- `DX de` rows retain exact frequency, callsign, summit reference, spotter and UTC. Mode is `UNKNOWN` unless the received comment states a mode.
- Valid references are matched through the existing SOTA catalogue so summit name and coordinates can drive the Portable map.
- The deprecated SOTAwatch API remains unused.

## Source corrections

- Home and Settings: redundant cards/build clutter removed; enhanced Shack entry; persisted EQ AUTO/SHOW/HIDE and Contest visibility with safe redirect/disarm; exact About build/schema/provider metadata.
- Contest: complete Setup, eight-panel Logging cockpit, bounded canonical Review, safe Network controls, readable tabs, canonical mutations and off-main latest-generation scoring.
- Band Maps: 160m–23cm selector, axes in all layouts, non-regulatory regional display segments, persisted bounded zoom/pan for touch/pointer/keyboard, deterministic lanes/stacks, improved Grid/Multi/Single context.
- Log Intelligence: immutable local/Wavelog authority identity, generation cancellation, exact prior UTC year, compact/colorful analytics, complete heatmap, and bounded valid 4/6/8-grid contact projection/repair.
- Tablet layouts: adaptive Presets header, wider DX feed with complete UTC/km essentials, adaptive Portable Places and coordinate-anchored MapLibre labels.
- Browser/Operations/Groups: minimum exact-host POTA/SOTA browser privileges; off-main inclusive cross-border POTA/SOTA catalog planning with explicit WWFF unavailability; actual Groups.io server timestamps with `TIME UNKNOWN`.
- Shared classification: one configured CS/DS resolver now drives Radio, DX, Band Maps and Neural/RF presentation with compact status text.

## Local validation evidence

- Android: 557 JVM tests, 0 failures; debug APK/AAB and debug Android-test APK assembled; instrumentation sources compiled; lint 0 errors, 177 warnings and 37 hints.
- Focused additions: 11 cases in two JVM files covering approved SOTA parsing/enrichment, exact-host browser policy, shared CS/DS mapping, inclusive cross-border planning, Groups.io timestamp truth and bounded Band Map layout.
- Rust: 97 passed, 0 failed, 1 ignored. Native core: 2/2 passed.
- Apple: unsigned generic iOS Simulator and generic iOS device builds succeeded.
- Release audits: contract/privacy/provenance, ITU/P.533 payload, Wavelog pinned paths, package size and deterministic scale/soak passed.
- Pre-final local artifact sizes were 112,078,976-byte APK and 53,332,066-byte AAB. Final SHA-bearing artifacts are rebuilt and recorded after the final source commit.
- Watchers: Wavelog 3.1.0/`af325614...` no change; MSHV no-review; OpenHamClock stable/release/licence pins unchanged with preview review-only delta; Nexus 1.7.6/`9fe27ec7...` review required. No upstream source was absorbed.

## Remaining evidence boundary

- Exact final pushed SHA must pass the seven-job hosted release-candidate workflow.
- Protected-tablet backup, replace install, UID/schema/data preservation, 120-second process survival and passive screenshots are pending the final exact-SHA artifacts.
- Authenticated services, audio, CAT/PTT/TUNE, transmit, on-air and RF behavior are not claimed by these source/build results.
- Frozen `main` remains `fb04d52df0c9ccc305125449bb188ef8e3f0185e`; this programme does not merge or push it.

## Device-gate correction

The first protected replace candidate at `b50a70fcd5394f2eb66d04db1e5379423d11ea32` installed with UID/data preservation but exited during launch. Sanitized evidence identified a bounded projection-grid repair query referencing nonexistent legacy column `q.grid`; canonical worked grid is stored in `q.details_json`. The query now uses `json_extract(q.details_json,'$.grid')`, and the existing Android migration-matrix test file includes an executable regression case that empties `grid_norm`, repairs from canonical JSON, and verifies the restored locator. No mutation occurred before the failing SELECT. The corrected commit, artifacts, hosted run and device evidence supersede the first candidate.

The next protected replace candidate at `e1e451aabcfdded36f161b373c3e58b44b4f49a3` also preserved UID and all 142 non-cache private files, but a DNS failure from the unavailable WWFF host escaped two sibling `async` requests during startup. The provider pair now runs in a `supervisorScope` and returns the failure as status data instead of cancelling the caller; a JVM regression test proves one failed provider request is captured while its sibling completes. This device finding supersedes that candidate and requires a new exact-SHA build, hosted run and protected replace check.
